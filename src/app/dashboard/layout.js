export const metadata = {
  title: "Dashboard",
  description: "ScrapCars customer portal to manage car selling requests and offers.",
};

export default function DashboardLayout({ children }) {
  return children;
}
